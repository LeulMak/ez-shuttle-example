import { BackButton } from "~/app/_components/back-button";
import { Vehicle } from "~/app/_core/models/vehicles/vehicles";

type Props = {
  quoteVehicles: Vehicle[];
};

export const VehicleChoice = (props: Props) => {
  const { quoteVehicles } = props;

  return <div></div>
};
